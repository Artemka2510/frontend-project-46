#!/usr/bin/env node

import { test, expect } from '@jest/globals';
import genDiff from '../src/genDiff.js';
import fs from fs;
import { fileURLToPath } from 'url';
import path, { dirname, dirname } from 'path';

test('gendiff tests', () => {
    
});